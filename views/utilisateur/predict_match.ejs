<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet"  >
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.2/css/all.min.css" />
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@300;400;700&display=swap" rel="stylesheet">
  <title>Make a prediction</title>
</head>
<body>
  <%-include('navbar')-%>
  
<center class="mb-4"><h2>  <i class="fa-solid fa-medal"></i> Make a prediction </h2></center>
         
<section class="container">
  <form method="post">
    <div class="container">
      <div class="row justify-content-center">
        <div class="col-6">
          <label><i class="fa-solid fa-futbol"></i> Choose a match</label>
          <select name="id_match" class="form-control" >
            <% for(match of matchs ) { %>
              <option value="<%=match.id_match%>">Team (A) <%=match.nom_a%>  VS Team (B) <%=match.nom_b%>  of (<%=new Date(match.date_match).toLocaleDateString() %>)</option>    
            <% } %>
          </select>
        </div>
      </div>
      <div class="row justify-content-center">
        <div class="col-3">
          <label><i class="fa fa-users" aria-hidden="true"></i> Predict the score for team A</label>
          <input type="text" name="teamA_score" class="form-control">
        </div>
        <div class="col-3">
          <label><i class="fa fa-users" aria-hidden="true"></i> Predict the score for team B</label>
          <input type="text" name="teamB_score" class="form-control">
        </div>
      </div>
      <div class="row justify-content-center mt-4">
        <div class="col-6 text-center">
          <button class="btn btn-success"><i class="fa-solid fa-floppy-disk"></i> Making a prediction</button>
        </div>
      </div>
    </div>
  </form>

 </section>

 <section class="mt-2">
  <center class="mb-2 mt-4"><h4>  <i class="fa-solid fa-list"></i> List of my predictions </h4></center>
 </section>
</body>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/js/bootstrap.bundle.min.js" integrity="sha384-HwwvtgBNo3bZJJLYd8oVXjrBZt8cqVSpeBNS5n7C8IVInixGAoxmnlMuBnhbgrkm" crossorigin="anonymous"></script>
</html>